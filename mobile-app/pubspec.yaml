name: agroai_mobile
description: "An AI Co-Pilot for Precision Agriculture mobile application."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^3.15.2
  firebase_storage: ^12.4.10
  cloud_firestore: ^5.6.12
  cloud_functions: ^5.6.2
  image_picker: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
